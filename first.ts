const a = "hello world"
 
const money: number =88;
const honey: string = 'ashik kaj suru kore daw'
const funny: boolean = true;

const age: number = 21;


// function declaration

function add (first: number, second: number) : number{
    const result = first + second;
    return result;
}
add(45,50);

// jodi amra return na kori tahole void use korte hobe

function doubleConsole(number:number): void {
   console.log(number *2);
}